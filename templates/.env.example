# Copy this file to .env and fill in the values your instructor provides.
# Server-side these same two variables are injected per slot, so your code never changes.
OPENAI_BASE_URL=https://your-llm-endpoint.example/v1
OPENAI_API_KEY=your-class-provided-key
