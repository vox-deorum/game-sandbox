# Copy this file to .env and fill in the credentials returned for your season.
# Server-side the endpoint and key are injected per slot, so your agent code does not change.
OPENAI_BASE_URL=
OPENAI_API_KEY=
