# Example: three_branches/sweeper

This internal example replaces the stand-still template with a villager that walks forward at half speed while using the `sweep` emote. It demonstrates the raw action dictionary without adding helper APIs before the student materials stage.

Compose it with:

```console
uv run python scripts/compose.py three_branches sweeper
```
