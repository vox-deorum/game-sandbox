# A minimal fixture tree for the local submission source tests. The source seam only resolves and
# reads a folder; manifest validation arrives in Stage 5.3, so this file's contents are arbitrary.
def act(observation):
    return 0
